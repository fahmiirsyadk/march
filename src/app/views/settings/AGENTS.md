# Settings view guidelines

- Status, result, error, or log-style helper text for a setting action must stay on the same `SettingRow` line as the control that produced it.
- Prefer inline placement immediately to the left of right-aligned buttons/controls; do not stack these messages underneath the control unless the whole setting intentionally uses a multi-line custom layout.
