name: Vouch - Check Issue

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  issues:
    types: [opened, reopened]

permissions:
  contents: read
  issues: write

jobs:
  check:
    runs-on: ubuntu-24.04
    steps:
      - uses: mitchellh/vouch/action/check-issue@v1.4.2
        with:
          issue-number: ${{ github.event.issue.number }}
          # Let unknown users open issues for now; only auto-close explicitly denounced users.
          require-vouch: false
          auto-close: true
          vouched-file: .github/VOUCHED.td
        env:
          GITHUB_TOKEN: ${{ github.token }}
